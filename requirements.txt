# Dependencias DIRECTAS del proyecto para desarrollo local.
#
# Antes esto era un `pip freeze` completo: 117 lineas donde las transitivas
# quedaban pineadas al mismo nivel que las que el codigo importa de verdad, y
# ademas colaba pywinpty, que solo existe en Windows y hacia fallar cualquier
# `pip install` sobre Linux. Ahora se declara solo lo que se importa y pip
# resuelve el resto.
#
# Las versiones estan fijadas a las verificadas en el entorno del equipo:
# el .pkl se serializa con joblib, y ese formato es sensible a la version de
# scikit-learn con la que se escribio.

# --------------------------------------------------------------- nucleo ML
joblib==1.5.3
matplotlib==3.11.1
numpy==2.4.6
pandas==2.3.3
PyYAML==6.0.3
scikit-learn==1.9.0
scipy==1.17.1

# ------------------------------------------------------------------- MLOps
# dvc[gdrive] en vez de dvc + dvc-gdrive sueltos: el extra garantiza que la
# version del backend case con la del core.
dvc[gdrive]==3.67.1
mlflow==3.15.1

# --------------------------------------------------------------------- API
fastapi==0.141.1
pydantic==2.13.4
uvicorn==0.52.4

# --------------------------------------------------------------- notebooks
ipykernel==7.3.0
jupyterlab==4.6.3

# ------------------------------------------------------------------- tests
# httpx lo necesita fastapi.testclient para las pruebas de la API.
httpx==0.28.1
pytest==9.1.1

# ----------------------------------------------------------- estilo (PEP8)
# black formatea, flake8 revisa.
black==25.9.0
flake8==7.1.1
